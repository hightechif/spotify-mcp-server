## 1. Modify Python Scripts to Use Relative dotenv Loading

- [x] 1.1 Update `spotify_auth.py` to load `.env` relative to the script's directory
- [x] 1.2 Update `get_token.py` to load `.env` relative to the script's directory

## 2. Update Documentation in README.md

- [x] 2.1 Replace the hardcoded path with environment variable reference in `README.md`
- [x] 2.2 Add instructions on setting the `SPOTIFY_MCP_DIR` environment variable and configuring Claude Desktop to expand it via a login shell
